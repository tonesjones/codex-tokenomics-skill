interface:
  display_name: "Tokenomics"
  short_description: "Recommend and verify cost-aware Codex routing"
  default_prompt: "Use $tokenomics to route this task efficiently."
policy:
  allow_implicit_invocation: true
